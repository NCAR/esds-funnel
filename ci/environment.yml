name: esds-funnel-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - catalogue
  - dask
  - fsspec
  - intake-esm
  - joblib
  - netcdf4
  - pip
  - pydantic
  - pytest
  - pytest-cov
  - pytest-sugar
  - pytest-xdist
  - xarray
  - zarr
  - pip:
      - -r ../requirements.txt
